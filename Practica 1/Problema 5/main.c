#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	float G;
	char L[]={};
	printf("Introdusca los grados y la inicial del nombre \n");
	scanf("%f",&G);
	scanf("%c",&L);
	if ("%c",L==c) {
		G=(9*G)/5+32;
		L=f;
	}
	else {
		G=(G-32)*(5/9);
		L=c;
	}
	printf("La conversion es de %f %c",G,L);
	return 0;
}
